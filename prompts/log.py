from __future__ import annotations

LOG_PROMPT = """## /log — 记录成就 Bullet

你的任务：将用户描述的经历改写为标准简历 bullet 并写入数据库。

⚠️ 一次只处理一条，写入成功后等用户提交下一条。

步骤：

0. **首次使用本会话的 /log 时**，先确认用户希望用什么语言记录 bullet：
   - 选项：`中文` / `英文` / `中英双语` / `其他语言`
   - 默认 = 当前用户与你对话所使用的语言（不要重复问已知答案，直接默认确认一下即可）
   - 一旦确定，本次会话后续 /log 沿用，除非用户主动要求切换

1. 调用 get_experiences() 列出已有经历，让用户选择关联到哪条经历（exp_id）。
   - 如果没有合适的经历，提示用户先用 /init 创建。

2. 询问用户给这条成就起个简洁名字（bullet_name，如「数据可视化」「性能优化」）。

3. 将用户描述按所选语言改写为标准 bullet：
   - **英文**：动词开头，过去式，≤ 20 词，体现结果不只是行为
   - **中文**：动词开头，简洁有力，≤ 30 字，体现结果
   - **中英双语**：分别给出中英两版，遵循各自风格
   - **其他语言**：参考英文风格的简洁原则

4. 量化追问逻辑：
   - 描述中有优化/改进但无具体数字 → 追问一次
   - 用户已有数字 / 纯技能描述 → 不追问

5. 确认后调用 log_bullet()：
   - exp_id：经历 ID
   - bullet_name：用户给的成就名
   - raw：用户原始描述，不要修改
   - rewritten：**dict 格式**，键为语言代码（zh / en / ja / ...），值为对应语言改写后的 bullet
     - 单语言示例：`{"en": "Built X to ..."}`
     - 双语示例：`{"en": "Built X to ...", "zh": "构建 X 以..."}`
   - category：achievement（有成果）/ skill（掌握技能）/ responsibility（日常职责）
   - has_number：是否包含量化数据
   - metric_values：量化数值列表（如有）

写入成功后只展示最终 rewritten bullet（多语言全部展示）。

可用工具：get_experiences, log_bullet
"""
